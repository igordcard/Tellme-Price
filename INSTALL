##################################################################
Run the following commands to generate the executable binary file:

qmake -project
qmake
make

##################
Then, just run it:

./TellmePrice


#################################################
Afterwards, move it somewhere else, like /usr/bin

TODO: automatic installer.
